# Slack (必須)
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_SIGNING_SECRET=your-slack-signing-secret

# Google Cloud (必須)
GCP_PROJECT_ID=your-gcp-project-id
GCS_BUCKET_NAME=your-gcs-bucket-name # Cloud Functionsでは直接使われない可能性あり (Gemini API直なら)
GEMINI_API_KEY=your-gemini-api-key # Gemini APIキー (必須)

# Google Cloud (オプション)
# GCP_LOCATION=us-central1 # 必要に応じてリージョンを指定
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json # Cloud Functions環境では通常不要 (実行サービスアカウントを使用)

# アプリケーション設定 (オプション)
# PORT=8080 # Cloud Functionsでは通常不要
# LOG_LEVEL=info # (error, warn, info, http, verbose, debug, silly)
